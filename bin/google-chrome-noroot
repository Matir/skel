#!/bin/bash

CHROME=`which google-chrome`

if [ `id -u` != "0" ] ; then
  exec "$CHROME" "$@"
fi

args=()
for x in "$@"; do
    args+=("$(printf %q "$x")")
done

su -c "$CHROME --user-data-dir=${HOME}/.chrome-data-dir ${args[*]}" chromeuser
